import { StyleSheet } from 'react-native'

export default () =>
  StyleSheet.create({
    homeScreen__containerView: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
    },
  })